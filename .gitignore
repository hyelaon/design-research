# 로컬 전용 설정 (개인 권한/환경)
.claude/settings.local.json

# 일일 생성물 — 스킬 실행 시 매번 새로 생성됨 (폴더는 유지)
data/*.json
boards/*.html

# 큐레이션한 레퍼런스 이미지 (개인 작업물·저작권물, 공유 대상 아님)
references/inbox/*
!references/inbox/.gitkeep

# OS / 에디터
.DS_Store
Thumbs.db
.vscode/
.idea/
